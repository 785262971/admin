package zp_test.springboot_server.zp.util;import org.joda.time.DateTime;import org.joda.time.Duration;import org.joda.time.format.DateTimeFormat;import org.joda.time.format.DateTimeFormatter;import java.util.Date;/** * Created by wangxiaowei on 17/5/24. */public class DateTimeUtil {    // 线程安全    public static final DateTimeFormatter YYYY_MM_DD_HH_MM_SS = DateTimeFormat            .forPattern("yyyy-MM-dd HH:mm:ss");    // 线程安全    public static final DateTimeFormatter YYYYMMDDHHMMSS = DateTimeFormat            .forPattern("yyyyMMddHHmmss");    // 线程安全    public static final DateTimeFormatter YYYYMMDD = DateTimeFormat            .forPattern("yyyyMMdd");    public static final DateTimeFormatter YYYYMM = DateTimeFormat            .forPattern("yyyyMM");    private DateTimeUtil(){    }    public static int getYM(long time){        return Integer.valueOf(YYYYMM.print(time));    }    /**     * 根据年月     * @param yyyyMM     * @param date     * @return     */    public static DateTime getDate(int yyyyMM, int date){        String dateString =Integer.toString(yyyyMM)+Integer.toString(date);        return YYYYMMDD.parseDateTime(dateString);    }    public static DateTime dateTimePlusMinutes(DateTime dateTime,int minutes){        return dateTime.plusMinutes(minutes);    }    public static long durationMillis(DateTime begin,DateTime end){        Duration d = new Duration(begin, end);        return d.getMillis();    }    public static long durationMillis(Date begin,Date end){        DateTime b= new DateTime(begin);        DateTime e= new DateTime(end);        Duration d = new Duration(b, e);        return d.getMillis();    }    public static String quartzCron(Date date,int minutes){        DateTime dt1 = new DateTime(date);        DateTime dt = dateTimePlusMinutes(dt1,minutes);        int year = dt.getYear();        int month = dt.getMonthOfYear();        int day = dt.getDayOfMonth();        int hour = dt.getHourOfDay();        int min = dt.getMinuteOfHour();        int sec = dt.getSecondOfMinute();        return sec+" "+min+" "+hour+" "+day+" "+month+" ? "+year;    }    public static void main(String [] args){        DateTime d1 = new DateTime("2012-05-01");        DateTime d2 = new DateTime("2012-05-02");        long l = durationMillis(d2,d1);        System.out.println(l);//        Date date = new Date();//        DateTime dt = new DateTime(date);////年//        int year = dt.getYear();////月//        int month = dt.getMonthOfYear();////日//        int day = dt.getDayOfMonth();////星期//        int week = dt.getDayOfWeek();////点//        int hour = dt.getHourOfDay();////分//        int min = dt.getMinuteOfHour();////秒//        int sec = dt.getSecondOfMinute();////毫秒//        int msec = dt.getMillisOfSecond();////        System.out.println(year+"--"+month+"--"+day+"--"+week+"--"+hour+"--"+min+"--"+sec+"--"+msec);//        System.out.println(dt.toString("yyyy-MM-dd HH:mm:ss SSS"));        //System.out.println(quartzCron(date));    }}